# Aethermind AgentOS - Production Environment Configuration
# Copy this file and configure with your production values

# =============================================================================
# PostgreSQL Database Configuration (Railway)
# =============================================================================
# Railway provides DATABASE_URL automatically, but you can also set individual vars
POSTGRES_USER=aethermind
POSTGRES_PASSWORD=<your-secure-password>
POSTGRES_DB=aethermind
POSTGRES_HOST=<railway-postgres-host>
POSTGRES_PORT=5432

# Railway auto-provides this, but format is:
# DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# =============================================================================
# Redis Configuration (Railway)
# =============================================================================
# Railway provides REDIS_URL automatically when you add Redis service
REDIS_URL=redis://<railway-redis-host>:6379

# =============================================================================
# LLM Provider API Keys
# =============================================================================
OPENAI_API_KEY=sk-your-openai-key-here
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here
GOOGLE_API_KEY=your-google-api-key-here

# =============================================================================
# Application Configuration
# =============================================================================
NODE_ENV=production

# Railway automatically sets PORT, but API uses this as fallback
PORT=3001
API_PORT=3001

# Logging level (error, warn, info, debug)
LOG_LEVEL=info

# =============================================================================
# Security Configuration
# =============================================================================
# CRITICAL: Generate a secure API key hash using: pnpm run generate-api-key
# This is REQUIRED in production
API_KEY_HASH=<generate-with-pnpm-run-generate-api-key>

# CRITICAL: JWT Secret for authentication tokens
# MUST be at least 32 characters, use a cryptographically secure random string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=<your-secure-jwt-secret-min-32-chars>

# =============================================================================
# CORS Configuration
# =============================================================================
# Comma-separated list of allowed origins (include your Vercel domain)
CORS_ORIGINS=https://tudominio.com,https://www.tudominio.com
ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com

# =============================================================================
# Rate Limiting
# =============================================================================
# 15 minutes in milliseconds
RATE_LIMIT_WINDOW_MS=900000
# Max requests per window
RATE_LIMIT_MAX_REQUESTS=100

# =============================================================================
# Performance & Scaling
# =============================================================================
# Database connection pool
DB_POOL_MAX=20

# LLM request timeout
LLM_TIMEOUT_MS=30000

# Task queue concurrency
QUEUE_CONCURRENCY=10

# Request body size limit
REQUEST_BODY_LIMIT=10mb

# =============================================================================
# Sentry (Error Tracking - Production only)
# =============================================================================
# Get your Sentry DSN from: https://sentry.io/settings/projects/
SENTRY_DSN=your-sentry-dsn-here

# =============================================================================
# Vercel Frontend Configuration (set in Vercel dashboard)
# =============================================================================
# NEXT_PUBLIC_API_URL=https://app.tudominio.com
